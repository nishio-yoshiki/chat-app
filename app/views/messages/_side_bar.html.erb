<div class="side-bar">
    <div class="side-bar-header">
      <div class="header-name">
        <a href="<%= edit_user_path(current_user) %>"><%= current_user.name %></a>
      </div>
      <div class="create-room">
        <a href="/rooms/new">チャットを作成する</a>
      </div>
   </div>
   <div class="rooms">
      <div class="room">
        <% @rooms.each do |room| %>
          <div class="room-name">
            <%= link_to room.name, room_messages_path(room) %>
          </div>
        <% end %>
      </div>
   </div>
</div>
